{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
<a href="{{path('newCategory')}}">Add Category</a>
Categories

{% for category in categories %}
    <a href="{{path('category', {id:category.id})}}">{{category.name}}</a>
{% endfor %}
{% endblock %}
